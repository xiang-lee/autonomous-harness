#!/usr/bin/env bash
set -euo pipefail

echo "This is a template .autonomous/init.sh"
echo "Replace it with repo-specific setup and startup commands."
echo "Example responsibilities: install dependencies, start the local app, print the local URL."
